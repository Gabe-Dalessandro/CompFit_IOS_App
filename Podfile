# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CompFit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for downloading images from urls
  pod 'SDWebImage'
  
  # Pods for AWS
  pod 'AWSCognito'
  pod 'AWSCore'
  pod 'AWSS3'
  
end
